name: ci

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  ci:
    uses: szymonrychu/tatara-operator/.github/workflows/ci-shared.yml@v1.36.1
    with:
      runner-label: tatara-memory
      smoke-binary: ./bin/tatara-memory
      # memory's build/smoke are pure Go (CGO_ENABLED=0), so the shared
      # `make`-only tool install is enough here (unlike cli/chat/ingester/
      # wrapper, which all build CGO-enabled).
      build-needs-cgo: false
      # memory owns its own image publish (build.sh's PUSH_SHORT_SHA_TAG
      # toggle, reused by release.yml to avoid a duplicate immutable Harbor
      # tag push), so the shared `image` job stays off here.
      enable-image: false
    secrets: inherit

  image:
    if: ${{ github.event_name == 'push' }}
    runs-on: tatara-memory
    needs: [ci]
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      HARBOR_USERNAME: ${{ secrets.HARBOR_USERNAME }}
      HARBOR_PASSWORD: ${{ secrets.HARBOR_PASSWORD }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: install buildctl
        run: |
          curl -sSfL "https://github.com/moby/buildkit/releases/download/v0.18.2/buildkit-v0.18.2.linux-amd64.tar.gz" \
            | sudo tar -xz -C /usr/local bin/buildctl
      - name: build and push image
        run: bash .github/ci/build.sh "${{ github.event.repository.name }}"
