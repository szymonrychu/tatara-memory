{{- /* No Secret is rendered: the generated Secret previously carried
   pg-password (always empty placeholder) and oidc-client-secret, neither
   of which is consumed by the tatara-memory binary. PG_DSN is projected
   inline from the cnpg cluster secret; OIDC is validated via issuer/audience
   only (no client secret). Use existingSecret to inject an operator-managed
   Secret if a future consumer is added. */ -}}
